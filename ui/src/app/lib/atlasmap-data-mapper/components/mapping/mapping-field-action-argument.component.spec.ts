/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { MappingFieldActionArgumentComponent } from './mapping-field-action-argument.component';

describe('MappingFieldActionArgumentComponent', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MappingFieldActionArgumentComponent],
    });
  });

  it(
    'should ...',
    inject([MappingFieldActionArgumentComponent], (service: MappingFieldActionArgumentComponent) => {
      expect(service).toBeTruthy();
    }),
  );
});
