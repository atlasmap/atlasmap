/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { MappingFieldActionComponent } from './mapping.field.action.component';

describe('MappingFieldActionComponent', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MappingFieldActionComponent],
    });
  });

  it(
    'should ...',
    inject([MappingFieldActionComponent], (service: MappingFieldActionComponent) => {
      expect(service).toBeTruthy();
    }),
  );
});
